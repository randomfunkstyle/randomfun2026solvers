[project]
name = "lmc"
version = "0.1.0"
description = "Little-Man compiler: Python-subset -> 2D grid language (POC)"
requires-python = "==3.12.*"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lmc"]
