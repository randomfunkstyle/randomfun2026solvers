[project]
name = "solvers"
version = "0.1.0"
description = "Local solver stubs / harness fixtures (built out in the external randomfun2026solvers repo)"
requires-python = "==3.12.*"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["solvers"]
