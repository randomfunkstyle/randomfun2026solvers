.lq/
tmp/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.DS_Store

# contest API key — never commit
.icfp-token

# agent worktrees and local session state
.claude/
.scratch/

# deadman-3d --wad outputs: derived from a locally owned retail IWAD — never commit
littleman/examples/local/

# IWAD-derived traffic trace: never committable (AGENTS.md, DOOM1.WAD rules)
scratch/deadman3d-opt/hires_traffic.json
