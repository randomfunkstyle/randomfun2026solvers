.lq/
tmp/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.DS_Store

# contest API key — never commit
.icfp-token

# Swift build output — never commit (module caches bake in absolute worktree paths)
solvers/swift/.build/

# agent worktrees and local session state
.claude/
